export declare enum EditableTargets {
    INPUT = "INPUT",
    TEXTAREA = "TEXTAREA",
    SELECT = "SELECT",
    CONTENT_EDITABLE = "contenteditable"
}
declare function isInputFocused(): boolean;
export default isInputFocused;
